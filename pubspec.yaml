name: agent_client_sdk
description: "Agent typed client SDK for Dart/Flutter over Connect (agent.v1.AgentService)."
version: 0.12.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  connectrpc: ^2.0.0
  protobuf: ^6.0.0
  fixnum: ^1.1.1
